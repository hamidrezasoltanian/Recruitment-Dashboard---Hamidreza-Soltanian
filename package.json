{
  "name": "recruitment-dashboard-fullstack",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack recruitment dashboard with React frontend and Node.js backend.",
  "scripts": {
    "postinstall": "npm install --prefix frontend && npm install --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "start:frontend": "npm start --prefix frontend",
    "start:fullstack": "concurrently \"npm:dev:backend\" \"npm:start:frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}
